// Package adapter contains the identity context's outbound adapters and
// inbound HTTP handlers: the Postgres implementation of
// domain.UserRepository, the first-run admin Provisioner, the SetupGuard
// middleware, and the OnboardingHandlers wizard the guard redirects to.
package adapter
