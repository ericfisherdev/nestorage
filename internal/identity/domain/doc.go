// Package domain holds the identity bounded context's domain model: the User
// aggregate, typed ids, the Role and UserColor enums, sentinel errors, and the
// UserRepository port. Persistence adapters live in the sibling adapter
// package.
package domain
